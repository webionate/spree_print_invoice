<% if @order.completed_at? %>

		<%= button_link_to t(:invoice_print), spree.admin_order_path(@order, :pdf , :template => 'invoice'), :icon => 'pdf' %>
	
		<%= button_link_to t(:packaging_slip_print), spree.admin_order_path(@order, :pdf , :template => 'packaging_slip'), :icon => 'pdf'  %>
	
		<%= button_link_to t(:credit_note_print), spree.admin_order_path(@order, :pdf , :template => 'credit_note'), :icon => 'pdf' %>
		
		<%#= button_link_to t(:edit), edit_admin_order_url(@order.number), :icon => 'edit' %>

<% end %>  
